language:
  - cpp

before_install:
  - pip install --user cpp-coveralls

addons:
  sonarcloud:
    organization: "1lae"
    token:
        secure: "fdeb00220627368bac4d08505890d79262edfb49"
    Dsonar.login="1LAE" -Dsonar.passwod="User89535462900"
    
script:
  - qmake
  - make
  - ./tests/tests

after_success:
  - coveralls --root . -E ".*external.*" -E ".*tests.*"
  - build-wrapper-linux-x86-64 --out-dir bw-output make clean all
  - sonar-scanner -Dsonar.verbose=true -X

cache:
 directories:
 - '$HOME/.sonar/cache'
 
notifications:
  email: false
